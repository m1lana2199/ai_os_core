version: '3.8'

services:
  ai-os-core:
    build: .
    container_name: ai_native_os_production
    restart: always
    ports:
      - "8080:8080"
    environment:
      - AI_OS_API_KEY=DEMO_KEY_GLOBAL
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1024M